galaxy_info:
  role_name: weave
  author: Till Klampaeckel
  description: The role will install weave on each host and create a network between them.
  company: Planetary Quantum GmbH
  license: Apache-2.0
  min_ansible_version: 2.9
  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:
  platforms:
    - name: EL
      versions:
        - 7
        - 8
    - name: Fedora
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - xenial
        - bionic
  galaxy_tags:
    - weave
    - weaveworks
    - docker
    - networking
    - swarm
    - kubernetes
    - k8s
    - container
dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
